numpy
PyMuPDF
requests
beautifulsoup4
EbookLib
soundfile
omnivoice

# Note: torch and torchaudio are omitted here because they are dynamically installed 
# by the batch script (run_omnivoice.bat) based on your system's CUDA GPU capabilities.
